import { Plugin } from 'vite';
export { default as pluginHelper, setPluginContext } from 'vite-plugin-bundle';

interface PluginOption {
    inline_worklet_paint?: boolean;
    inline_worklet_audio?: boolean;
    inline_worklet_layout?: boolean;
    inline_worklet_animation?: boolean;
    service_worker_file?: string;
}
declare let isBuild: boolean;
declare function betterWorker({ inline_worklet_paint, inline_worklet_audio, inline_worklet_layout, inline_worklet_animation, service_worker_file }: PluginOption): Plugin[];

export { PluginOption, betterWorker as default, isBuild };
